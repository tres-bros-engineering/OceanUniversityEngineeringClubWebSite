const BASE_URL = "http://localhost:3001";

const ApiRoutes = {
    SUPERADMIN : `${BASE_URL}/superadmin`,
    ADMIN : `${BASE_URL}/admin`,
    ARTICLE : `${BASE_URL}/article`,
    NEWS : `${BASE_URL}/news`,
    COMMENT : `${BASE_URL}/comment`
}

export default ApiRoutes;